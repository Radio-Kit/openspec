# Tasks

- [ ] 1. Revert `_scheduleNotifyListeners()` to `notifyListeners()` in `DeviceProvider.setInputValue()`
- [ ] 2. Remove debug `debugPrint` from `setInputValue` (BLE write timing warning)
- [ ] 3. Remove debug `debugPrint` from `_onWidgetValueChanged` (touch tick logging)
- [ ] 4. Keep `_listEquals` skip optimization (already in place)
- [ ] 5. Build and install app
- [ ] 6. Flash firmware and verify slider controls brightness in real-time
